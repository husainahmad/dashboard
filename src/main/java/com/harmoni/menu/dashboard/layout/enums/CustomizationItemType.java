package com.harmoni.menu.dashboard.layout.enums;

/**
 * Kinds of items managed in the customization editor.
 */
public enum CustomizationItemType {
    /** A customization group. */
    CUSTOMIZATION,
    /** A selectable option within a customization. */
    OPTION,
    /** A tier-specific price applied to an option. */
    TIER_PRICE
}